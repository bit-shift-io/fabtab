[build]
# 1. Install dependencies, then 2. Run your build script
command = "pip install -r requirements.txt && python build.py"

# Replace "dist" with whatever folder your build.py outputs your final static files to
publish = "public"

[build.environment]
PYTHON_VERSION = "3.11"
